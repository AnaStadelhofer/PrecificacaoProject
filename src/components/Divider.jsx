import { View } from 'react-native';

const Divider = () => {
  return (
    <View
      style={{
        borderBottomColor: 'grey',
        borderBottomWidth: 1,
        paddingTop: 30,
      }}
    />
  );
};

export default Divider;